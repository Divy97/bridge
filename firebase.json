{
    "firestore": {
      "rules": "firestore.rules"
    },
    "storage": {
      "rules": "storage.rules"
    },
    "functions": {
      "source": "packages/functions",
      "predeploy": "pnpm --filter @bridge/functions build"
    }
  }